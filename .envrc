# Reload shell when npins changes its source graph.
watch_file npins/sources.json npins/default.nix

# Loads default.nix automatically when entering this directory.
# First run: `direnv allow`.
use nix
