<script lang="ts">
  import IllustrationAnimee from '../../accueil/animation/IllustrationAnimee.svelte';
  import SectionBadge from './SectionBadge.svelte';

  const scènes = [SectionBadge];
</script>

<IllustrationAnimee {scènes} étiquette="Une barre de progression complétée et le badge Cyberdépart débloqué." />
